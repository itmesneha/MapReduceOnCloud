name: Deploy Backend to EC2

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'deployment/**'
      - '.github/workflows/**'
  workflow_dispatch:  # Allow manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to EC2
      env:
        EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
      run: |
        # Setup SSH
        mkdir -p ~/.ssh
        echo "$EC2_SSH_KEY" > ~/.ssh/ec2_key.pem
        chmod 600 ~/.ssh/ec2_key.pem
        ssh-keyscan -H 18.138.254.59 >> ~/.ssh/known_hosts
        
        # Deploy to EC2
        ssh -i ~/.ssh/ec2_key.pem ubuntu@18.138.254.59 << 'ENDSSH'
          # Check if repo exists, if not clone it
          if [ ! -d ~/MapReduceOnCloud ]; then
            echo "📥 Cloning repository..."
            git clone https://github.com/itmesneha/MapReduceOnCloud.git ~/MapReduceOnCloud
          fi
          
          cd ~/MapReduceOnCloud
          echo "📥 Pulling latest changes..."
          git pull origin main
          
          # Check if Docker is installed
          if ! command -v docker &> /dev/null; then
            echo "🔧 Installing Docker..."
            sudo apt update
            sudo apt install -y apt-transport-https ca-certificates curl software-properties-common
            curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
            echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
            sudo apt update
            sudo apt install -y docker-ce docker-ce-cli containerd.io
            sudo systemctl start docker
            sudo systemctl enable docker
            sudo usermod -aG docker $USER
          fi
          
          # Check if Docker Compose is installed
          if ! command -v docker-compose &> /dev/null; then
            echo "🔧 Installing Docker Compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
          fi
          
          cd deployment
          
          echo "🛑 Stopping existing containers..."
          sudo docker-compose down 2>/dev/null || true
          
          echo "🔨 Building Docker image..."
          sudo docker-compose build --no-cache
          
          echo "▶️  Starting containers..."
          sudo docker-compose up -d
          
          echo "✅ Deployment complete!"
        ENDSSH
        
    - name: Health Check
      run: |
        echo "⏳ Waiting for API to be ready..."
        sleep 15
        
        # Try up to 5 times
        for i in {1..5}; do
          if curl -f http://18.138.254.59:8000/docs; then
            echo "✅ Deployment successful and API is responding!"
            exit 0
          fi
          echo "⏳ Attempt $i/5 - waiting..."
          sleep 10
        done
        
        echo "⚠️ API might not be fully ready yet, but deployment completed."
        echo "Check manually at: http://18.138.254.59:8000/docs"
